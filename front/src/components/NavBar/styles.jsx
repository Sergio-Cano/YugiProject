import { styled } from "styled-components";

const Section = styled.section`
    width: 90vw;
    height: 5vh;
    display: flex;
    justify-content: space-between;
    margin: 5%;
`

const NavOption = styled.h1``

const User = styled.p``

export default {
    Section,
    NavOption,
    User
}